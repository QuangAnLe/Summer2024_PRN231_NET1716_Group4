@page
@model ClientMilkTeamPage.Pages.UserPage.CommentPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comment[0].CommentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment[0].Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment[0].Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment[0].TeaID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment[0].UserID)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Comment)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CommentDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Content)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rating)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TeaID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserID)
                </td>
                
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.CommentID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.CommentID">Details</a> |
                </td>
            </tr>
        }
    </tbody>
</table>
